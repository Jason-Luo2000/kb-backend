{
  "name": "@pi-ext/kb",
  "version": "1.0.0",
  "private": true,
  "description": "pi extension for kb-backend (uses kb_client 1.0: idempotent retry + structured errors)",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
