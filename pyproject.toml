[project]
name = "kb-backend"
version = "0.1.0"
description = "Enterprise KB backend — dual-path retrieval (summary navigation + vector), pi agent front-end"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.29",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
  "psycopg[binary]>=3.1",
  "sqlalchemy>=2.0",
  "elasticsearch>=8.13",
  "minio>=7.2",
  "redis>=5.0",
  "httpx>=0.27",
  "python-multipart>=0.0.9",
  "pdfplumber>=0.11",
  "python-docx>=1.1",        # T13：DOCX 解析
  "python-pptx>=0.6",        # T13：PPTX 解析
  "openpyxl>=3.1",           # T13：XLSX 解析
  "beautifulsoup4>=4.12",    # T13：HTML 解析
  "lxml>=5.1",               # T13：bs4 后端 + docx 解析
  "prometheus_client>=0.20", # T16：/metrics 监控
  "anthropic>=0.28",
  "tiktoken>=0.7",
  "slowapi>=0.1.9",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "ruff"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
